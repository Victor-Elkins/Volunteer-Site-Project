import { F as Faker } from '../airline-WjISwexU.cjs';

/**
 * The faker instance for the `th` locale.
 *
 * - Language: Thai
 * - Endonym: ไทย
 *
 * This instance uses the following locales internally (in descending precedence):
 *
 * - `th`
 * - `en`
 * - `base`
 */
declare const faker: Faker;

export { faker };
